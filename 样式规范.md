# 新能源工具箱 · 样式与布局开发规范

> 版本：V1.0（2026-09-08）
> 适用范围：本目录下所有网页工具（`新能源工具箱V1.0.html` 主框架 + `tools/` 内全部工具）
> 用途：新增工具、修改界面时**必须遵循**本规范，保证全部工具两套主题（暗色科技风 / 浅色工程风）观感一致。

---

## 1. 目录结构约定

```
1-新能源工具箱/
├── 新能源工具箱V1.0.html        主框架（侧边导航 + iframe 内容区 + 首页卡片）
├── 样式规范.md                  本文件
├── tools/                      工具页目录（文件名 = 首页侧边栏命名，一一对应）
│   ├── 2000坐标与经纬度互转.html
│   ├── CAD展点.html
│   ├── 风机基础正八边形征地坐标生成器.html
│   ├── XY与AB坐标转换.html
│   ├── 导线输送容量计算.html
│   ├── 新能源项目环境效益计算表.html
│   └── 批量新建文件夹.html
└── css/
    ├── dark/                   暗色科技风（每工具一个 .css + index.css）
    │   ├── index.css
    │   └── <工具名>.css ×5
    └── light/                  浅色工程风（与 dark/ 同名同结构）
        ├── index.css
        └── <工具名>.css ×5
```

规则：

1. **一个工具 = 一个单文件 HTML**（只含结构与 JS 逻辑），**样式全部外置**到 `css/dark/` 与 `css/light/`，两套 CSS **选择器完全一致**，仅变量值与颜色不同。
2. 工具 HTML **不放任何 `<style>` 块、不写内联颜色**（`display:none`、布局尺寸类内联样式允许）。
3. 需要主题感知的颜色一律用 CSS 变量或 `var(--xxx)`，**禁止在 HTML/JS 中硬编码十六进制颜色**。
4. 新工具文件名必须与主框架 `新能源工具箱V1.0.html` 中 TOOLS 注册表的 `name` 字段、侧边栏 `nv-name` 完全一致。

---

## 2. 主题机制

### 2.1 存储与加载

- 主题值存 `localStorage["nbx-theme"]`：`"dark"`（默认）| `"light"`。
- 同时在 `<html>` 上设置 `data-theme="dark|light"` 属性。
- 每个页面 `<head>` 中放置**主题加载器**（在首帧渲染前执行，避免闪屏）：

```html
<link id="themeCss" rel="stylesheet" href="../css/dark/<工具名>.css">
<script>
(function(){
  try {
    var t = localStorage.getItem("nbx-theme") === "light" ? "light" : "dark";
    document.getElementById("themeCss").href = "../css/" + t + "/<工具名>.css";
    document.documentElement.setAttribute("data-theme", t);
  } catch (e) {}
})();
</script>
```

> 注意：主框架在根目录，路径前缀为 `css/`；`tools/` 内工具用 `../css/`。

### 2.2 主题切换按钮

- **主框架**：顶栏右侧 `#themeBtn` 按钮，切换时更新 `localStorage`、替换自身 CSS、并重载 iframe（`toolFrame.src = toolFrame.src`），使工具页读取新主题。
- **工具独立打开时**：`</body>` 前注入浮动按钮 `.theme-toggle`（右下角 36px 圆形），点击换 CSS。
- **在主框架 iframe 内时按钮自动隐藏**，判断方式：

```js
if (window.parent !== window) return;   // iframe 内不显示切换按钮
```

### 2.3 两主题的视觉定义

| | 暗色科技风（dark） | 浅色工程风（light） |
|---|---|---|
| 页面底色 | `#070b14` + 青色径向光晕 + 40px 网格线 | `#f0f2f5` / `#eef2f8` 纯色 |
| 主色 | 青色霓虹 `#22d3ee`（强调 `#38bdf8`） | 深蓝 `#1f4e79`（强调 `#2d6da3`） |
| 卡片 | 深蓝渐变玻璃 `#0f1623→#0a0f1c` | 纯白 `#ffffff` |
| 正文 | `#e6edf6` / 次要 `#aebbd0` / 弱化 `#7d8da6` | `#2c3e50` / 次要 `#55637a` / 弱化 `#7a8aa0` |
| 边框 | `rgba(56,189,248,.14)` / 强边框 `.28` | `#d0dce8` / 强边框 `#b8c7d6` |

---

## 3. 设计令牌（Design Tokens）

每个工具 CSS 的 `:root` 中必须定义以下变量（值按主题取下表对应列）：

```css
:root{
  --r-lg: 10px;    /* 页头 / 卡片 / 面板 圆角 */
  --r-md: 8px;     /* 按钮 / 输入框 / 表格容器 / 提示条 / tabs 圆角 */
  --r-sm: 6px;     /* 表格内紧凑控件（select/input 小尺寸）圆角 */
  --th-bg:  ...;   /* 表头背景 */
  --th-tx: ...;    /* 表头文字色 */
  --focus-ring: ...; /* 输入聚焦环 */
}
```

| 令牌 | 暗色值 | 浅色值 |
|---|---|---|
| `--r-lg / --r-md / --r-sm` | `10px / 8px / 6px` | 同左 |
| `--th-bg` | `rgba(34,211,238,.12)` | `#eaf1f8` |
| `--th-tx` | `#7dd3fc` | `#1f4e79` |
| `--focus-ring` | `0 0 0 2px rgba(34,211,238,.18)` | `0 0 0 2px rgba(31,78,121,.15)` |

---

## 4. 界面细则

### 4.1 页头（每个工具顶部，统一横幅）

- 结构：标题 `h1`（必有）+ 副说明行（`.subtitle` / `p` / `.sub`，可选）。
- 样式：卡片式横幅，内距 `14px 18px`（CAD 因标题+说明分两块，用 `12px 16px` + 下接说明条），圆角 `--r-lg`。

```css
/* 暗色 */
header{ background:linear-gradient(135deg,rgba(34,211,238,.16),rgba(56,189,248,.08));
        border:1px solid var(--line2); border-radius:var(--r-lg); padding:14px 18px; }
h1{ font-size:20px; font-weight:700; letter-spacing:1px; color:#fff;
    text-shadow:0 0 18px rgba(34,211,238,.45); }

/* 浅色 */
header{ background:linear-gradient(120deg,#2F5496,#3b6cb5);
        border:none; border-radius:var(--r-lg); padding:14px 18px; }
h1{ font-size:20px; font-weight:700; letter-spacing:1px; color:#fff; text-shadow:none; }
```

- 副说明文字：暗色 `#aebbd0` 12.5px；浅色 `rgba(255,255,255,.82)` 12.5px。
- 版本徽章：胶囊形（`border-radius:20px`，11px 字号）。

### 4.2 面板 / 卡片

- 圆角 `--r-lg`，1px 边框，`overflow:hidden`（标题条直角贴合圆角）。
- 面板标题条：内距 `10px 14px`、13.5px/700、主色文字、底部 1px 分隔线、背景为表头色弱化版（暗 `rgba(34,211,238,.08)` / 浅 `#eaf1f8`）。
- 已有内边距结构改造时用**负边距标题条**：`margin:-16px -16px 12px;`（参考风机工具）。

### 4.3 按钮（三级）

统一基准：内距 `8px 18px`、圆角 `--r-md`、字号 13.5px/600、`transition:all .15s`、1px 边框。

| 级别 | 暗色 | 浅色 |
|---|---|---|
| 主按钮 | `linear-gradient(135deg,#22d3ee,#38bdf8)` + 深字 `#06283d` + 辉光 | `#1f4e79` 实底 + 白字，hover `#2d6da3` |
| 幽灵按钮 | 透明底 + 1px `--line2` 边框 + `#aebbd0` 字，hover 变主色 | 白底 + 1px `#b8c7d6` + `#55637a` 字 |
| 小按钮 `.small` | `5px 12px`、12px 字号，其余同上 | 同左 |
| 成功类（复制/下载） | `rgba(52,211,153,.14)` 底 + `.35` 边框 + `#34d399` 字 | `#e8f5e9` 底 + `#a5d6a7` 边框 + `#2e7d32` 字 |
| 禁用 | `#1c2739` 底 `#55637a` 字 | `#e5eaf0` 底 `#9aa7b5` 字 |

工具栏内紧凑按钮（如导线工具 toolbar）：内距 `6px 14px`、圆角 `--r-sm`、12.5px 字号。

### 4.4 表格

- 表头 `th`：`--th-bg` 底 + `--th-tx` 字、12.5px/600、内距 `8px 10px`（左右布局表格用 `9px 12px`）。
- 裸表格（无外容器）需给表头补圆角：`th:first-child{border-top-left-radius:var(--r-md)}` 及对应的右上角。
- 行：斑马纹（暗 `rgba(56,189,248,.04)` / 浅 `#fafcfe`）+ 悬停（暗 `rgba(34,211,238,.08)` / 浅 `#eef4fa`）。
- 边框：暗 `rgba(56,189,248,.14)` / 浅 `#d0dce8`。
- 数字列统一 `font-family:Consolas,monospace` + 右对齐（`.num`）；暗色数字 `#7dd3fc` / 浅色 `#0f5b9e`。
- **数字列的表头必须同样右对齐**：数据格选择器写成 `td.num{…}`，并补 `th.num{text-align:right}`，表头单元格加 `class="num"`。否则表头贴左/居中、数字贴右，视觉错位（`2000坐标与经纬度互转`、`CAD展点` 均踩过）。注意 `th.num` **只改对齐**，不要把 `.num` 写成无前缀选择器，否则表头会被套上数据格的等宽字体与数字配色。
- 表格容器（包裹层）圆角 `--r-md` + 1px 边框 + `overflow:auto`。
- 长表格容器内的 sticky 表头背景必须用**实色**（暗 `#0a0f1a`），不能用透明。

### 4.5 输入框 / 下拉 / 文本域

- 圆角 `--r-md`，1px 边框（暗 `--line2` / 浅 `#b8c7d6`），内距 `6px 10px`（独立）/ `5px 8px`（表内）。
- 聚焦：`border-color: 主色; box-shadow: var(--focus-ring);`，`outline:none`。
- placeholder：暗 `#4b5970` / 浅 `#94a3b8`。
- **必须补 `select option{ background:卡片色; color:文字色; }`**（否则下拉展开是系统白底）。
- 复选/单选：`accent-color: 主色`。
- 数值输入框右对齐 + 等宽字体（工程惯例）。

### 4.6 提示 / 消息条

- 圆角 `--r-md`，1px 彩色半透明边框 + 同色系 10% 底 + 实色文字。
- 成功 `#34d399` / 警告 `#fbbf24` / 错误 `#f87171`；浅色对应 `#2e7d32` / `#b26a00` / `#c0392b`。

### 4.7 滚动条与细节

- 全局滚动条 8px，滑块暗 `rgba(56,189,248,.3)` / 浅 `#c0cdd9`，圆角 4px，轨道透明。
- 高亮单元格（HTML 内联 `background:#fbbf24` 的强调格）两主题各自用 CSS 属性选择器覆盖：
  `td[style*="fbbf24"]{ background:... !important; color:... !important; }`
- 图标/emoji 按钮圆角 50%（主题切换按钮 36px）。

---

## 5. 工具 HTML 骨架模板

新增工具时复制此骨架，填入 `<工具名>` 与正文：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><工具标题></title>
<link id="themeCss" rel="stylesheet" href="../css/dark/<工具名>.css">
<script>
(function(){
  try {
    var t = localStorage.getItem("nbx-theme") === "light" ? "light" : "dark";
    document.getElementById("themeCss").href = "../css/" + t + "/<工具名>.css";
    document.documentElement.setAttribute("data-theme", t);
  } catch (e) {}
})();
</script>
</head>
<body>
  <!-- 页头横幅 -->
  <header>
    <h1><工具标题></h1>
    <p class="sub">一句话说明：输入 / 输出 / 依据规范</p>
  </header>

  <!-- 面板：panel + 标题条 + panel-body -->
  <div class="panel">
    <div class="panel-title">① 输入</div>
    <div class="panel-body">
      <!-- 表单控件 -->
    </div>
  </div>

  <script>
  (function(){
    "use strict";
    /* 计算逻辑（IIFE 封装，不污染全局） */
  })();
  </script>
  <script>
  /* 主题切换按钮：仅独立打开时显示（原样复制，勿改动） */
  (function(){
    if (window.parent !== window) return;
    var btn = document.createElement("button");
    btn.className = "theme-toggle";
    btn.type = "button";
    function sync(){
      var t = document.documentElement.getAttribute("data-theme") === "light" ? "light" : "dark";
      btn.textContent = (t === "light") ? "\ud83c\udf19" : "\u2600\ufe0f";
    }
    btn.addEventListener("click", function(){
      var t = document.documentElement.getAttribute("data-theme") === "light" ? "dark" : "light";
      document.documentElement.setAttribute("data-theme", t);
      try { localStorage.setItem("nbx-theme", t); } catch (e) {}
      var link = document.getElementById("themeCss");
      if (link) link.href = "../css/" + t + "/<工具名>.css";
      sync();
    });
    sync();
    document.body.appendChild(btn);
  })();
  </script>
</body>
</html>
```

---

## 6. 新增工具接入清单

1. `tools/<工具名>.html` 按第 5 节骨架创建（**name 与文件名一致**）。
2. 编写 `css/dark/<工具名>.css` 与 `css/light/<工具名>.css`：从任一现有工具 CSS 复制 `:root` 与公共段（滚动条 / 页头 / 按钮 / 表格 / 提示条 / 主题切换按钮），只替换业务布局部分。
3. `新能源工具箱V1.0.html` 的 `TOOLS` 注册表加条目（key 用英文 slug，`file` 指向 `tools/<工具名>.html`；**插入位置须与侧边栏 nav 位置一致**，首页卡片按注册表顺序渲染）。
4. `新能源工具箱V1.0.html` 侧边栏加导航按钮（`data-page` = slug；按"测量·坐标 / 电气·环保 / 实用工具"归组），并同步更新首页"共 N 个工具"计数。
5. 验收（见第 7 节）。

## 7. 验收清单（改样式后必查）

- [ ] 暗色、浅色两主题分别打开，逐页截图对比：页头横幅、按钮三级、表头颜色、圆角完全一致。
- [ ] 表格表头与数据列对齐：逐列比对「对齐边」偏差（右对齐列比右边缘、居中列比中心、左对齐列比左边缘），≤3px 视为合格；不可只靠目测。可用 `Range.selectNodeContents(th/td)` 取文本包围盒测量。
- [ ] 主框架切换主题后，iframe 内工具**同步**换肤；刷新页面主题保持。
- [ ] 工具独立双击打开：右下角切换按钮可用；iframe 内打开时该按钮不可见。
- [ ] 所有 `select` 下拉展开后底色为深色/白色主题色，无系统白底突兀。
- [ ] 输入框聚焦出现主题聚焦环；数值校验失败红色边框正常。
- [ ] 计算功能回归：填入示例数据 → 结果正确 → 复制/下载功能正常。
- [ ] HTML 中无硬编码颜色（`grep "color:#" tools/*.html` 仅允许 `var(--` 与注释命中）。
